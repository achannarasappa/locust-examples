{
  "name": "apartment-listings",
  "version": "1.0.0",
  "description": "Example Locust web scraper to collect the latest 100 Craigslist apartment home listings in New York City",
  "main": "src/handler.js",
  "scripts": {
    "build": "rm -f ./infra/src.zip && npm install && zip -r ./infra/src.zip ./src package*.json node_modules",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ani Channarasappa",
  "license": "MIT",
  "dependencies": {
    "@achannarasappa/locust": "^1.3.0",
    "aws-sdk": "^2.814.0",
    "moment": "^2.24.0",
    "pg": "^7.12.1"
  }
}
